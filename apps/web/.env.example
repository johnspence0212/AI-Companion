# Prefer a relative URL so auth cookies stay same-origin.
VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=EnterpriseStarter

# Optional. Default `npm run dev` does not register a service worker.
# Use `npm run dev:pwa` (or VITE_PWA_DEV=true) to test install locally.
# VITE_PWA_DEV=true
